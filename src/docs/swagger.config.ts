import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'User Service',
  description: 'Api Services for Auth Services',
  version: '1.0',
  tags: ['healthcheck', 'user'],
};
